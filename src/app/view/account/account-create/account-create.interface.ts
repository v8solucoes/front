import { Idata, Irequest } from '@domain/interface'

/* export type Iconta = Idata['adm']['conta']

export type InewAccount = 
Pick<Iuser,'accessType'> &
 Pick<Irequest,'itemId'> &
 Pick<Irequest,'domain'> */