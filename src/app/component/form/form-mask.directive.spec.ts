import { FormMaskDirective } from './form-mask.directive';

describe('FormMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new FormMaskDirective();
    expect(directive).toBeTruthy();
  });
});
